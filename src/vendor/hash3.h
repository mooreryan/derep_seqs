#define RANK3 3
#define WSIZE 256
#define OUTPUT(j) count++

#include <assert.h>
#include <stdlib.h>
#include <string.h>
/* #include <stdio.h> */

int hash3_search(unsigned char *x, int m, unsigned char *y, int n);
